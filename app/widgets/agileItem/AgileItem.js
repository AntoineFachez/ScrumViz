import { Box } from '@mui/material';
import React from 'react';

export default function AgileItem({ url }) {
  return;
  <Box>
    <Box>AgileItem</Box>
    <Box>
      <img src={url} alt="hello" />
    </Box>
  </Box>;
}
